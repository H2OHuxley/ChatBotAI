# ChatbotAI

Simple AI-powered chatbot using:

- **Python + Flask** for the backend
- **HTML, CSS, JavaScript** for the frontend
- **OpenAI API** for generating responses (or any OpenAI-compatible provider)

---

## 📁 Project Structure

```text
backend/
├─ app.py          # Flask backend (main file)
├─ .env            # Environment variables (gawa ka env )
├─ .gitignore
├─ templates/
│  └─ index.html   # Frontend chat UI (HTML + inline CSS/JS)
├─ static/         # (Optional) extra CSS/JS files
├─ venv/           # Python virtual environment (ignored)
└─ README.md       # This file

Install venv tapos pip install OpenAi
